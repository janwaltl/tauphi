[package]
name = "perf_event"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "^0.2"
thiserror = "1.0.40"
tokio = { version = "1.28.1", features = ["full"] }

[build-dependencies]
cc = "1.0"
